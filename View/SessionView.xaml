<UserControl x:Class="DMAssistant.View.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:DMAssistant.View"
             xmlns:h="clr-namespace:DMAssistant.Helpers"
             xmlns:c="clr-namespace:DMAssistant.Helpers.Converter"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.WPF.DragDrop"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <c:PlusOneConverter x:Key="PlusOneConverter"/>
    </UserControl.Resources>
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Session list -->
        <StackPanel Grid.Column="0">
            <Button Content="New Session" Command="{Binding AddSessionCommand}" Margin="5"/>
            <ListBox ItemsSource="{Binding Sessions}" 
                     SelectedItem="{Binding SelectedSession}" 
                     DisplayMemberPath="Name"
                     Background="#EEE"
                     Margin="5"/>
        </StackPanel>

        <!-- RIGHT: Session editor -->
        <DockPanel Grid.Column="1" Margin="0,5,0,0" LastChildFill="True">

            <!-- Session Name -->
            <TextBox DockPanel.Dock="Top"
                     Text="{Binding SelectedSession.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="18"
                     FontWeight="Bold"
                     IsEnabled="{Binding SelectedSession, Converter={StaticResource NullToBoolConverter}}"/>

            <!-- TabControl fills remaining space automatically -->
            <TabControl SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">

                <TabItem Header="Scenes">
                    <RichTextBox h:RichTextBoxHelper.BoundDocument="{Binding Scenes, Mode=TwoWay}" AcceptsReturn="True"/>
                </TabItem>

                <TabItem Header="Secrets">
                    <StackPanel HorizontalAlignment="Center">
                        <Button Content="Add Secret" Width="100" Margin="0,0,0,8" Command="{Binding AddSecretCommand}" />
                        <ListBox ItemsSource="{Binding Secrets}"
                                 dd:DragDrop.IsDragSource="True"
                                 dd:DragDrop.IsDropTarget="True"
                                 AlternationCount="999" >
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="900"/>
                                            <ColumnDefinition Width="24"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Number prefix -->
                                        <TextBlock Margin="0,0,4,0" Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock.Text>
                                                <Binding RelativeSource="{RelativeSource AncestorType=ListBoxItem}" Path="(ItemsControl.AlternationIndex)" Converter="{StaticResource PlusOneConverter}" StringFormat="{}{0}. "/>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <!-- Checkbox -->
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Grid.Column="1" />

                                        <!-- Editable text -->
                                        <TextBox Grid.Column="2" Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="6,0,6,0" AcceptsReturn="True" TextWrapping="Wrap"/>

                                        <!-- Delete button -->
                                        <Button Grid.Column="3" Command="{Binding DataContext.RemoveSecretCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Height="30">
                                            <Image Source="/Resources/icon-delete.png" Height="16"/>
                                        </Button>
                                    </Grid>

                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                    </StackPanel>
                </TabItem>
                
                <TabItem Header="Locations">
                    <v:LocationPanelView DataContext="{Binding LocationPanel}"/>
                </TabItem>

                <TabItem Header="NPCs">
                    <v:NPCPanelView DataContext="{Binding NPCPanel}" />
                </TabItem>

                <TabItem Header="Items">
                    <v:ItemPanelView DataContext="{Binding ItemPanel}"/>
                </TabItem>

                <TabItem Header="Encounters">
                    <v:EncountersPanelView DataContext="{Binding EncountersPanel}"/>
                </TabItem>

                <TabItem Header="Notes">
                    <RichTextBox h:RichTextBoxHelper.BoundDocument="{Binding Notes, Mode=TwoWay}" AcceptsReturn="True"/>
                </TabItem>

            </TabControl>

        </DockPanel>

    </Grid>
</UserControl>
