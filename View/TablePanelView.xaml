<UserControl x:Class="DMAssistant.View.TablePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Margin="0,0,0,10"/>
            
            <Button Content="New Table"
                 Grid.Row="1"
                 Command="{Binding AddTableCommand}"
                 Margin="0,0,0,10"/>

            <TextBlock Text="Tables"
                    Grid.Row="2"
                    FontWeight="Bold"
                    Margin="0,0,0,5"/>

            <ListBox ItemsSource="{Binding TablesView}"
                  Grid.Row="3"
                 SelectedItem="{Binding SelectedTable}">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>

                            <!-- Remove button -->
                            <Button Command="{Binding DataContext.DeleteTableCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                     CommandParameter="{Binding}"
                     Margin="0"
                     DockPanel.Dock="Left">
                                <Image Width="16" Height="16" Source="pack://application:,,,/Resources/icon-delete.png"/>
                            </Button>

                            <TextBlock Text="{Binding Name}" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="5"/>

                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>
        </Grid>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="2" CornerRadius="5">
            <ContentControl Content="{Binding SelectedTable}" />
        </Border>


    </Grid>
</UserControl>
