<UserControl x:Class="DMAssistant.View.CampaignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="CampaignPanel">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left menu buttons -->
        <StackPanel Grid.Column="0" Margin="0, 5, 0, 0">
            <Button Content="Campaign Details" Command="{Binding ShowCampaignDetails}"/>
            <Button Content="Player Characters" Command="{Binding ShowPCsCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="NPCs" Command="{Binding ShowNPCsCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Fronts" Command="{Binding ShowFrontsCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Locations" Command="{Binding ShowLocationsCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Items" Command="{Binding ShowItemsCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Spells" Command="{Binding ShowSpellsCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Lore" Command="{Binding ShowLoreCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Monsters" Command="{Binding ShowMonstersCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="Notes" Command="{Binding ShowNotesCommand}" Margin="0, 10, 0, 0"/>
            <Button Content="World Map" Command="{Binding ShowWorldMapCommand}" Margin="0, 10, 0, 0"/>
        </StackPanel>

        <!-- Content area for campaign modules -->
        <ContentControl Content="{Binding CurrentModuleView }" Grid.Column="1" Background="White" Margin="5"/>
    </Grid>
</UserControl>
