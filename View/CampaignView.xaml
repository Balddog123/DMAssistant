<UserControl x:Class="DMAssistant.View.CampaignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="CampaignPanel">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left menu buttons -->
        <StackPanel Background="#EEE" Grid.Column="0">
            <Button Content="Player Characters" Command="{Binding ShowPCsCommand}"/>
            <Button Content="NPCs" Command="{Binding ShowNPCsCommand}"/>
            <Button Content="Fronts" Command="{Binding ShowFrontsCommand}"/>
            <Button Content="Locations" Command="{Binding ShowLocationsCommand}"/>
            <Button Content="Items" Command="{Binding ShowItemsCommand}"/>
            <Button Content="Lore" Command="{Binding ShowLoreCommand}"/>
            <Button Content="Monsters" Command="{Binding ShowMonstersCommand}"/>
            <Button Content="Notes" Command="{Binding ShowNotesCommand}"/>
            <Button Content="World Map" Command="{Binding ShowWorldMapCommand}"/>
        </StackPanel>

        <!-- Content area for campaign modules -->
        <ContentControl Content="{Binding CurrentModuleView }"
                    Grid.Column="1"
                    Background="White"/>
    </Grid>
</UserControl>
