<UserControl x:Class="DMAssistant.View.SpellsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:h="clr-namespace:DMAssistant.Helpers.Converter"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <h:BoolToAsteriskConverter x:Key="BoolToAsteriskConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Spell List + Add Button -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Margin="0,0,0,10"/>
            <!-- New Spell -->
            <Button Content="New Spell"
                    Grid.Row="1"
                    Command="{Binding AddSpellCommand}"
                    Margin="0,0,0,10"/>

            <ComboBox Grid.Column="0" Grid.Row="2" ItemsSource="{Binding SpellLevelsView}" SelectedItem="{Binding SelectedLevel}" Margin="0,0,10,10"/>

            <TextBlock Text="Spells"
                       Grid.Row="3"
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>

            <ListBox ItemsSource="{Binding SpellsView}"
                     Grid.Row="4"
                    SelectedItem="{Binding SelectedSpell}">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>

                            <!-- Remove button -->
                            <Button Command="{Binding DataContext.DeleteSpellCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                        CommandParameter="{Binding}"
                        Margin="0"
                        DockPanel.Dock="Left">
                                <Image Width="16" Height="16"
                           Source="pack://application:,,,/Resources/icon-delete.png"/>
                            </Button>

                            <!-- Spell name + level -->
                            <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" Margin="5">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1}){2}">
                                        <Binding Path="Name"/>
                                        <Binding Path="Level"/>
                                        <Binding Path="IsNew" Converter="{StaticResource BoolToAsteriskConverter}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>

                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>
        </Grid>

        <!-- RIGHT: Spell Detail View -->
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="2" CornerRadius="5">
            <ContentControl Content="{Binding SelectedSpell}" />
        </Border>
        

    </Grid>
</UserControl>
