<UserControl x:Class="DMAssistant.View.CombatTrackerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- Top row: Round display + next button -->
            <RowDefinition Height="Auto"/>
            <!-- Bottom row: Combatants list -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Round display with next round button -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Round text -->
            <TextBlock Text="{Binding CurrentRound, StringFormat=Round {0}}" 
                       FontSize="20" 
                       VerticalAlignment="Center" 
                       FontWeight="Bold"/>

            <!-- Next round button -->
            <Button Grid.Column="1" Command="{Binding NextRoundCommand}" Width="30" Height="30" Margin="10,0,0,0">
                <Image Source="pack://application:,,,/Resources/icon-arrow.png"/>
            </Button>
        </Grid>

        <!-- Combatants List -->
        <ListView Grid.Row="1" ItemsSource="{Binding CombatItems}" SelectedItem="{Binding SelectedCombatItem}">
            <ListView.View>
                <GridView>
                    <!-- Initiative (editable) -->
                    <GridViewColumn Header="Init" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Initiative, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Name (editable) -->
                    <GridViewColumn Header="Name" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- Type (read-only) -->
                    <GridViewColumn Header="Enc. Item" Width="70" DisplayMemberBinding="{Binding EncounterItemName}"/>
                    <!-- Type (read-only) -->
                    <GridViewColumn Header="Type" Width="70" DisplayMemberBinding="{Binding CombatItemType}"/>

                    <!-- HP (editable) -->
                    <GridViewColumn Header="HP" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding CurrentHP, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Max HP (read-only) -->
                    <GridViewColumn Header="Max HP" Width="60" DisplayMemberBinding="{Binding MaxHP}" />

                    <!-- Armor Class (read-only) -->
                    <GridViewColumn Header="AC" Width="50" DisplayMemberBinding="{Binding ArmorClass}" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
