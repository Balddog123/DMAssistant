<UserControl x:Class="DMAssistant.View.CampaignLoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MaxWidth="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ROWS FOR LEFT COLUMN -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LEFT COLUMN CONTROLS -->
        <Button Grid.Column="0" Grid.Row="0"
            Content="New Lore"
            Command="{Binding AddLore}"
            Margin="5"/>

        <TextBlock Grid.Column="0" Grid.Row="1"
               Text="Lore"
               FontWeight="Bold"
               Margin="5"/>

        <ListBox Grid.Column="0" Grid.Row="2"
             ItemsSource="{Binding LoreView}"
             SelectedItem="{Binding SelectedLore}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <Button Command="{Binding DataContext.DeleteLore, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}"
                            DockPanel.Dock="Left">
                            <Image Width="16"
                               Height="16"
                               Source="pack://application:,,,/Resources/icon-delete.png"/>
                        </Button>

                        <TextBlock Text="{Binding Name}" Margin="5" DockPanel.Dock="Right" VerticalAlignment="Center"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- RIGHT SIDE -->
        <Border Grid.Column="1" Grid.Row="0" Grid.RowSpan="4" BorderBrush="Gray" BorderThickness="1" Margin="2"  CornerRadius="5">
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" FontSize="24" Text="{Binding SelectedLore.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                <TextBox Grid.Row="1" Text="{Binding SelectedLore.Description, Mode=TwoWay}" Margin="5"/>
            </Grid>
            
        </Border>

    </Grid>

</UserControl>
