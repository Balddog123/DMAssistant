<UserControl x:Class="DMAssistant.View.NPCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel Margin="10">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Delete Button -->
            <Button Grid.Column="1"
            Content="Delete"
            Background="Red"
            Foreground="White"
            FontWeight="Bold"
            Padding="5,2"
            Margin="10,0,0,0"
            VerticalAlignment="Center"
            Command="{Binding DeleteCommand}"/>
        </Grid>
        <Grid>

            <!-- ================= RIGHT DETAILS PANEL ================= -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Name -->
                <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding NPC.Name, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="0" Grid.Column="1" Margin="0,5"/>

                <!-- Race -->
                <TextBlock Text="Race:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding AvailableRaces}" 
                  SelectedItem="{Binding NPC.Race, UpdateSourceTrigger=PropertyChanged}" 
                  Grid.Row="1" Grid.Column="1" Margin="0,5" />

                <!-- Description -->
                <TextBlock Text="Description:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                <TextBox Text="{Binding NPC.Description, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="2" Grid.Column="1" Margin="0,5" AcceptsReturn="True" Height="80" VerticalScrollBarVisibility="Auto"/>

                <!-- Goal -->
                <TextBlock Text="Goal:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                <TextBox Text="{Binding NPC.Goal, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="3" Grid.Column="1" Margin="0,5" AcceptsReturn="True" Height="60" VerticalScrollBarVisibility="Auto"/>

                <!-- Home -->
                <TextBlock Text="Home:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                <TextBox Text="{Binding NPC.Home, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="4" Grid.Column="1" Margin="0,5" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
            </Grid>

        </Grid>
    </DockPanel>
    
</UserControl>
