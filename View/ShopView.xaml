<UserControl x:Class="DMAssistant.View.ShopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView ItemsSource="{Binding ItemsView}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Name" Width="Auto" DisplayMemberBinding="{Binding Name}" />
                <GridViewColumn Header="Cost" Width="Auto" DisplayMemberBinding="{Binding Item.Cost}" />
                <GridViewColumn Header="Rarity" Width="Auto" DisplayMemberBinding="{Binding Item.Rank}" />
                <GridViewColumn Header="Type" Width="Auto" DisplayMemberBinding="{Binding Item.Type}" />
                <GridViewColumn Header="Function" Width="Auto" DisplayMemberBinding="{Binding FunctionText}" />
                <GridViewColumn Header="Appearance" Width="Auto" DisplayMemberBinding="{Binding AppearanceText}" />
                <GridViewColumn Header="Origin" Width="Auto" DisplayMemberBinding="{Binding OriginText}" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
