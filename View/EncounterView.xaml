<UserControl x:Class="DMAssistant.View.EncounterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Encounter Name Field -->
        <TextBox Grid.Row="0"
                 Text="{Binding Name, Mode=TwoWay}"
                 FontSize="20"
                 FontWeight="Bold"
                 Margin="0 0 0 5" />

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <!-- Left side: Encounter Items -->
                <ColumnDefinition Width="Auto" />
                <!-- Right side: Tracker + Item Details -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="0.5*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding TotalCR}"/>
                <TextBlock Text="{Binding TotalXPComparison}"/>
                <TextBlock Text="{Binding TotalDifficultyRating}"/>
            </StackPanel>
                <!-- List of Encounter Items -->
            <Grid Grid.Column="0" Grid.Row="1">
                <Grid.RowDefinitions>
                    <!-- Buttons -->
                    <RowDefinition Height="Auto" />
                    <!-- Encounter Item list -->
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0">
                    <Button Content="Add Monster" Margin="0,0,1,0" Command="{Binding AddMonsterCommand}"></Button>
                    <Button Content="Add Event" Margin="1,0,0,0" Command="{Binding AddEventCommand}"></Button>
                    <Button Content="Run!" Margin="1,0,0,0" Command="{Binding RunEncounterCommand}"></Button>
                </DockPanel>
                
                <!-- EncounterItems list -->
                <ListView Grid.Row="1" ItemsSource="{Binding EncounterItems}" SelectedItem="{Binding SelectedItem}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Amount" Width="50" DisplayMemberBinding="{Binding QuantityDisplay}" />
                            <GridViewColumn Header="Round#" Width="50" DisplayMemberBinding="{Binding RoundNumber}" />
                            <GridViewColumn Header="Type" Width="50" DisplayMemberBinding="{Binding TypeName}" />
                            <GridViewColumn Header="" Width="40">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                                            <Image Width="16" Height="16" Source="pack://application:,,,/Resources/icon-delete.png"/>
                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>

            <!-- Combat Tracker control placeholder -->
            <Border Grid.Column="0" Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1" Background="#FAFAFA">
                <ContentControl Content="{Binding CurrentCombat}" />
            </Border>

            <!-- EncounterItem Details control placeholder -->
            <Border Grid.Column="1" Grid.Row="2" Grid.RowSpan="2" BorderBrush="#CCCCCC" BorderThickness="1">
                <ContentControl Content="{Binding SelectedEncounterItemViewModel}" />
            </Border>

        </Grid>

    </Grid>

</UserControl>
