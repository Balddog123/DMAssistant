<UserControl x:Class="DMAssistant.View.WorldMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer HorizontalScrollBarVisibility="Auto"  VerticalScrollBarVisibility="Auto"  PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
        <Border BorderThickness="1" BorderBrush="Gray" Margin="2">

            <Grid>
                <Grid.LayoutTransform>
                    <ScaleTransform
                    ScaleX="{Binding ZoomLevel}"
                    ScaleY="{Binding ZoomLevel}" />
                </Grid.LayoutTransform>

                <Button Command="{Binding OpenMapCommand}">
                    <Image Source="{Binding Map.FullThumbnailData,Converter={StaticResource ByteArrayToImageConverter}}" Stretch="Uniform" />
                </Button>
            </Grid>

        </Border>
    </ScrollViewer>

</UserControl>
