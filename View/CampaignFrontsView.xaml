<UserControl x:Class="DMAssistant.View.CampaignFrontsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- TextBox -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
               Text="Fronts" 
               FontWeight="Bold" 
               FontSize="16" 
               Margin="0,0,0,5" HorizontalAlignment="Center"/>

        <!-- Editable TextBox -->
        <TextBox Grid.Row="1" 
             Text="{Binding Fronts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
             AcceptsReturn="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"/>
    </Grid>

</UserControl>
