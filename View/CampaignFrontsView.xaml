<UserControl x:Class="DMAssistant.View.CampaignFrontsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
               Text="Fronts" 
               FontWeight="Bold" 
               FontSize="16" 
               Margin="0,5,0,5" HorizontalAlignment="Center"/>

        <Button Grid.Row="1" Margin="5" HorizontalAlignment="Left" Content="Add New" Command="{Binding AddNewFrontCommand}"/>

        <ListBox ItemsSource="{Binding Fronts}" Grid.Row="2"
         ScrollViewer.CanContentScroll="False">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="2">
                        <Button DockPanel.Dock="Right" Width="30" Height="30"
                        Command="{Binding DataContext.RemoveFrontCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                        CommandParameter="{Binding}">
                            <Image Source="/Resources/icon-delete.png"/>
                        </Button>
                        <TextBox Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalAlignment="Top"
                         MinHeight="60"
                         VerticalScrollBarVisibility="Auto"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>



    </Grid>

</UserControl>
