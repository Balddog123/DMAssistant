<UserControl x:Class="DMAssistant.View.LocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             xmlns:rc="clr-namespace:DMAssistant.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <ScrollViewer Margin="10">
        <DockPanel>
            <!-- Top: Name + Delete button -->
            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Name TextBox -->
                <TextBox Grid.Column="0"
                     Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     FontSize="22" 
                     FontWeight="Bold"
                     VerticalAlignment="Center"/>

                <!-- Delete Button -->
                <Button Grid.Column="1"
                    Content="Delete"
                    Background="Red"
                    Foreground="White"
                    FontWeight="Bold"
                    Padding="5,2"
                    Margin="10,0,0,0"
                    VerticalAlignment="Center"
                    Command="{Binding DeleteCommand}"/>
            </Grid>

            <!-- Main content: Image + Description side by side -->
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.75*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="0.25*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Height="Auto">
                    <Grid HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Set Image From File" Grid.Column="0" Margin="2" Command="{Binding SetImageFromFile}"/>
                        <Button Content="Paste Image" Grid.Column="1" Margin="2" Command="{Binding SetImageFromClipboard}"/>
                    </Grid>

                    <Border MinHeight="400"
                    Background="LightGray"
                    Margin="0,0,0,0"
                    VerticalAlignment="Center"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="5">
                        <Image Source="{Binding LocationImage}" Stretch="Uniform"/>
                    </Border>
                </StackPanel>


                <!-- Description Stack -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Description" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="400"/>
                </StackPanel>
            </Grid>

            <!-- Map Section -->
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Height="150" Margin="0, 20, 0, 0">
                <StackPanel>
                    <Button Content="Add New Map" Width="100" HorizontalAlignment="Left" Command="{Binding AddNewMapCommand}"/>
                    <ListBox ItemsSource="{Binding Maps}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="1" BorderBrush="Gray" Margin="2">
                                    <Image Width="100" Height="100"
                                            Source="{Binding ThumbnailData, Converter={StaticResource ByteArrayToImageConverter}}">
                                        <Image.InputBindings>
                                            <MouseBinding MouseAction="LeftClick"
                                                          Command="{Binding DataContext.OpenMapCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                          CommandParameter="{Binding}" />
                                        </Image.InputBindings>
                                    </Image>

                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
                
            </ScrollViewer>



        </DockPanel>
    </ScrollViewer>
</UserControl>
