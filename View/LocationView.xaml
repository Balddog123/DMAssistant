<UserControl x:Class="DMAssistant.View.LocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMAssistant.View"
             xmlns:rc="clr-namespace:DMAssistant.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBox
                Grid.Row="0"
                Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                FontSize="22" 
                FontWeight="Bold"
                MinWidth="100"
                HorizontalAlignment="Center"
                TextAlignment="Center"/>

            <!-- Main content: Image + Description side by side -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Margin="2" Command="{Binding SetImageFromFile}">
                            <Image Source="/Resources/icon-folder.png"/>
                        </Button>
                        <Button Grid.Column="1" Margin="2" Command="{Binding SetImageFromClipboard}">
                            <Image Source="/Resources/icon-paste.png"/>
                        </Button>
                    </Grid>

                    <Border MinHeight="400"
                            MinWidth="400"
                            MaxHeight="800"
                            MaxWidth="800"
                            HorizontalAlignment="Left"
                            BorderThickness="1">
                        <Image Source="{Binding LocationImage}" Stretch="Uniform"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="10, 10, 0, 0">
                    <TextBlock Text="Description" 
                           FontWeight="Bold" 
                           Margin="5"/>
                    <TextBox
                        Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        VerticalScrollBarVisibility="Auto"
                        MinHeight="600"/>
                </StackPanel>
            </Grid>

            <!-- Map Section -->
            <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0, 20, 0, 0">
                <StackPanel >
                    <Button HorizontalAlignment="Left" Command="{Binding AddNewMapCommand}">
                        <Image Source="/Resources/icon-add.png"/>
                    </Button>
                    <ListBox Height="125" ItemsSource="{Binding Maps}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="30"/>
                                        <RowDefinition Height="80"/>

                                    </Grid.RowDefinitions>
                                    <Image Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Grid.RowSpan="2" Width="110" Height="110" Source="{Binding ThumbnailData, Converter={StaticResource ByteArrayToImageConverter}}">
                                        <Image.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.OpenMapCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" />
                                        </Image.InputBindings>
                                    </Image>
                                    <Button Grid.Column="1" Grid.Row="0" Command="{Binding DataContext.RemoveMap, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                        <Image Source="/Resources/icon-delete.png" Height="16"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
                
            </ScrollViewer>



        </Grid>
    </ScrollViewer>
</UserControl>
